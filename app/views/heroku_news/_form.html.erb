<%= form_for(@heroku_news) do |f| %>
  <% if @heroku_news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@heroku_news.errors.count, "error") %> prohibited this heroku_news from being saved:</h2>

      <ul>
      <% @heroku_news.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
